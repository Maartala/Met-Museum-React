const SearchBar = () => {
    return (
        <section>
            <input type="text" placeholder="search for art" />
            <button >Search</button>
        </section>
    );
}

export default SearchBar;